# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                    			   Dashboard.index
GET     /room/{roomId}/                    Dashboard.index

GET     /room/{roomId}/deposit             Deposit.index(int roomId)
POST    /room/{roomId}/deposit/additem     Deposit.addItem(int roomId)
GET     /room/{roomId}/deposit/deleteitem  Deposit.deleteitem(int roomId)

GET     /room/{roomId}/grocery             Grocery.index(int roomId)
GET     /room/{roomId}/dashboard           Dashboard.index(int roomId)
GET     /room/{roomId}/tasks               Task.index(int roomId)
GET     /room/{roomId}/tasks               Task.index(int roomId)


GET     /room/{roomId}/profile             Profile.index
POST    /room/{roomId}/profile/updateroomy Profile.updateroomy(int roomId)
POST    /room/{roomId}/profile/updatephoto Profile.updateroomy(int roomId)

GET     /whoWeAre                          Application.whoWeAre
GET     /reachUs                           Application.reachUs
GET     /whatWeDo                          Application.whatWeDo


# Ignore favicon requests
GET     /favicon.ico                       404

# Map static resources from the /app/public folder to the /public path
GET     /public/                           staticDir:public

# Catch all
*       /{controller}/{action}             {controller}.{action}

#Import Secure routes
*       /                                  module:secure 
